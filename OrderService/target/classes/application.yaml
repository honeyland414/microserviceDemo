server:
  port: 8082
spring:
  application:
    name: orderservice

tracing:
  url: http://192.168.66.129:4318/v1/traces
management:
  tracing:
    sampling:
      probability: 1.0
logging:
  pattern:
    level: '%5p [${spring.application.name:},%X{traceId:-},%X{spanId:-}]'